services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile

    environment:
      - ODOO_URL=https://gader-odoo-web.trzy2m.easypanel.host
      - ODOO_DB=gader_live_db_new
    networks:
      - podcast-network

networks:
  podcast-network:
    driver: bridge
